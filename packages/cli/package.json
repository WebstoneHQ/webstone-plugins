{
  "name": "@webstone/cli",
  "version": "0.0.3",
  "description": "The Webstone command line",
  "bin": {
    "webstone": "./bin",
    "ws": "./bin"
  },
  "scripts": {
    "build": "node ./scripts/esbuild.js build",
    "clean": "rm -fr ./dist ./node_modules",
    "dev": "node ./scripts/esbuild.js dev",
    "prepublishOnly": "pnpm build",
    "prepare": "pnpm build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Nikles, @mikenikles",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.11.19"
  }
}
