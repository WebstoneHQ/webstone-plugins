/**
 * Can be made globally available by placing this
 * inside `app.d.ts` and removing `export` keyword
 */
export interface Locals {
  // TODO: Provide your app's data. See https://kit.svelte.dev/docs#hooks-handle for examples.
}
