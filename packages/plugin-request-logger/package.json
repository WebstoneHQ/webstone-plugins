{
	"name": "webstone-plugin-request-logger",
	"version": "0.1.0",
	"description": "Template Plugin for Webstone - PLEASE UPDATE THIS",
	"private": true,
	"scripts": {
		"build": "pnpm --recursive --parallel build",
		"changeset": "changeset && pnpm install",
		"clean": "pnpm --recursive --parallel clean",
		"dev": "pnpm --recursive --parallel dev",
		"format": "pnpm --recursive format",
		"preinstall": "npx only-allow pnpm",
		"release": "changeset publish"
	},
	"keywords": [
		"webstone",
		"plugin"
	],
	"author": "Cahllagerfeld",
	"license": "MIT",
	"devDependencies": {
		"@changesets/cli": "^2.26.0",
		"rimraf": "^3.0.2"
	}
}
